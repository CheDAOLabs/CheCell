import type { Abi, DeclareSignerDetails, DeployAccountSignerDetails, Invocation, InvocationsSignerDetails, Signature, SignerInterface, typedData } from "starknet";
import type { IStarknetRpc } from "./starknet.model";
export declare class StarknetRemoteSigner implements SignerInterface {
    private wallet;
    constructor(wallet: IStarknetRpc);
    getPubKey(): Promise<string>;
    signMessage(typedData: typedData.TypedData, accountAddress: string): Promise<Signature>;
    signTransaction(_transactions: Invocation[], _transactionsDetail: InvocationsSignerDetails, _abis?: Abi[]): Promise<Signature>;
    signDeployAccountTransaction(_transaction: DeployAccountSignerDetails): Promise<Signature>;
    signDeclareTransaction(_transaction: DeclareSignerDetails): Promise<Signature>;
}
